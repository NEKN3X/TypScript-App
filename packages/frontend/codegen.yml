overwrite: true
schema: ${GRAPHQL_SCHEMA}
documentse: "./graphql/**/*.graphql"
generates:
  ./graphql/generated.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
